name: DEPI Final Project

on:
  push:
  workflow_dispatch:

env:
  AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
  AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
  AWS_REGION: ${{ vars.AWS_REGION }}
  SSH_PRIVATE_KEY: ${{ secrets.SSH_PRV_KEY }}
  SSH_PUBLIC_KEY: ${{ secrets.SSH_PUB_KEY }}

jobs:
  Deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up AWS credentials
        uses: aws-actions/configure-aws-credentials@v1
        with:
          aws-access-key-id: ${{ env.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ env.AWS_SECRET_ACCESS_KEY }}
          aws-region: ${{ env.AWS_REGION }}

      - name: Install Terraform
        uses: hashicorp/setup-terraform@v2

      - name: Install SSH & Ansible
        run: |
          sudo apt-get update && sudo apt-get install -y openssh-server ansible

      - name: Create Files for SSH keys
        run: |
             echo "${{ env.SSH_PRIVATE_KEY }}" > .ssh/id_aws_ec2
             echo "${{ env.SSH_PUBLIC_KEY }}" > .ssh/id_aws_ec2.pub

      - name: Terraform Init
        run: |
          cd ./terraform
          terraform init

      - name: Terraform Apply (manual approval)
        run: |
          cd ./terraform
          sudo terraform apply 
                          -var="key_name_value=${{ env.SSH_PUBLIC_KEY }}" \
                          -var="aws_region=${{ env.AWS_REGION }}" \
                          -auto-approve

      - name: Gather EC2 public IP
        run: |
            terraform output -raw public_ip > public_ip.txt
            export EC2_PUBLIC_IP=$(cat public_ip.txt)
            echo "EC2_PUBLIC_IP=$EC2_PUBLIC_IP"
                  

      - name: Run Ansible playbook
        run: |
              cd ./ansible
              echo "ec2_host ansible_host=$EC2_PUBLIC_IP" > hosts
              ansible-playbook main.yml